# CvetyOnline

Online shop-window.

## Deploying backend

Install gems:

```bash
bundle install
```

Then run project locally:

```bash
rails server
```

## Deploying frontend

Install npm packages:

```bash
npm install
```

Then run project locally:

```bash
npm start
```
